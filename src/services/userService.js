const { User } = require('../database/models');

module.exports = {
  login: async ({ email, password }) => {
  },

  create: async ({ email, password }) => {

  },

  findAll: async () => {

  },

  findByPk: async (id) => {

  },

  destroy: async (id) => {

  },
};
